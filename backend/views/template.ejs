<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" type="css" href="../../frontend/static/templatestyle.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> <%= pageTitle %> </title>
</head>


<body>
    <header>
    <nav>
        <a href="/" > Home</a>  
        
        <% if (loggedIn) { %>
            <span>Welcome <%= user.username.username %> TO THE JUNGLE!</span>
        <% } else { %>
            <a href="/login"> Login</a>
        <% } %>
       
        <a href="/signup" > Sign Up</a>
        <a href="/game" > Game</a>
        <a href="/lobby" > Lobby</a>
    </nav>
    </header>
    <main>
        <%= pageContent %>
    </main>
</body>
</html>